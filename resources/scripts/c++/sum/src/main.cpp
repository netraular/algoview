#include <sum.hpp>

int	main(int argc, char **argv)
{
	(void) argc;
	// while (true)
		// std::cout << atoi(argv[1]) << " + 1 = " << sum(atoi(argv[1])) << std::endl;
		std::cout << sum(atoi(argv[1])) << std::endl;

}